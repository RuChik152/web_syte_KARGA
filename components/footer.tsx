import styles from "../styles/Home.module.scss";

const Footer = () => {
    return (
        <footer style={{height: "100vh"}} className={styles.footer}>
                <p><span>&#169;</span> BeliVR 2022</p>
        </footer>
    );
};

export default Footer;